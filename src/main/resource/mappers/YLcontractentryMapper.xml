<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yulan.dao.YLcontractentryDao">





    <select id="getYLcbyStateandmarketName" resultType="HashMap">
        select
        *

        from(
        select rownum rm, a.*from(select  count(*) nums ,state, marketName,SUBMARKETMANAGERNAME from ylcontractentry group by state , marketName,SUBMARKETMANAGERNAME) a
        ) tm where rm &gt;= #{start} and rm &lt;= #{number }

    </select>

    <select id="getYLcBySate" resultType="HashMap">
        select count(*) nums ,state from ylcontractentry t group by state
    </select>

    <select id="count" resultType="int">
        select count (*) from (select * from ylcontractentry group by state , marketName,SUBMARKETMANAGERNAME)
    </select>

</mapper>
